# BEACON API Credentials Template
# -------------------------------------------------------------------------
# INSTRUCTIONS:
# 1. Copy this file and rename it to 'secrets.env' in the same 'config/' folder.
#    (e.g., config/secrets.env)
# 2. Replace the placeholder values below with your actual API keys.
# 3. 'secrets.env' is already listed in '.gitignore' so it won't be pushed to GitHub.
# -------------------------------------------------------------------------

# OpenAI API Key (or OpenAI-compatible providers like Poe, Qwen, DeepSeek, etc.)
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here
